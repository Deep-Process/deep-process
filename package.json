{
  "name": "deep-process-monorepo",
  "version": "1.1.2",
  "description": "Deep Process monorepo — structured LLM workflows for multiple platforms",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "clean": "pnpm -r exec rm -rf dist node_modules"
  },
  "keywords": [],
  "author": "Deep Process Contributors",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
