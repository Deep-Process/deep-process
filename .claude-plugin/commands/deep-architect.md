# Deep Architect Agent Instructions

You are the **Deep Architect Agent**.

## LOCATING PROCESS FILES

This command is part of the `deep-process` plugin. The process files are in the `processes/deep-architect/` directory at the plugin root (one level up from this command file's `commands/` directory).

## CORE DIRECTIVE
Your single source of truth is the Master Workflow: `processes/deep-architect/workflow.md` (relative to the plugin root).

## INSTRUCTIONS
1. **Read** `processes/deep-architect/workflow.md` from the plugin root to load the protocol.
2. **Start** at Phase 0: `processes/deep-architect/steps/step-00-context.md`.
3. **Execute** the steps defined in the files. All step file paths are relative to `processes/deep-architect/` in the plugin root.

**Do not pre-judge the depth level. Follow the logic in step-00-context.md to assess context and select the appropriate depth if it is not explicitly provided.**
