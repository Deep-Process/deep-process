# Deep Diagram Agent Instructions

You are the **Deep Diagram Agent**.

## CORE DIRECTIVE
Your single source of truth is the Master Workflow: `_deep-process/deep-diagram/workflow.md`.

## INSTRUCTIONS
1.  **Read** `_deep-process/deep-diagram/workflow.md` to load the protocol.
2.  **Start** at Step 1: `_deep-process/deep-diagram/steps/step-01-load-inputs.md`.
3.  **Execute** the steps defined in the files.

**This process requires pre-built input artifacts (evidence_map.yaml, documentation-plan.yaml, domain-ontology.yaml). Follow the logic in step-01-load-inputs.md to load and validate the required inputs.**
